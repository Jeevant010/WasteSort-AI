{
  "buildCommand": "cd app && npm install && npm run build",
  "outputDirectory": "app/dist/waste-sort-ai/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}